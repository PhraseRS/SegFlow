# ============================================================
# RS-Seg-GUI —— Layer A: GUI 宿主环境依赖
# ============================================================
# 仅包含运行 GUI 本体所需的依赖（数据管理 / 可视化 / 配置生成）。
# 训练与推理所需的深度学习框架请见 environment_train.yml，
# 在独立的 conda 环境中安装。
#
# 安装：
#   pip install -r requirements.txt
#
# Python 版本要求：>= 3.9（推荐 3.9 或 3.10）
# ------------------------------------------------------------

# --- 硬性必须依赖（缺失则无法启动）---
PySide6>=6.5.0          # GUI 框架（Widget / 信号 / 线程）
numpy>=1.24.0           # 图像数组运算、掩码计算、样本统计
opencv-python>=4.8.0    # 图像读写、缩放、颜色空间转换
Pillow>=10.0.0          # 大图懒加载、PNG/JPEG 读取
matplotlib>=3.7.0       # 类别分布图绘制
Jinja2>=3.1.0           # 配置文件模板生成

# --- 可选但强推荐依赖（缺失时 .tif 大图无法正常显示）---
rasterio>=1.3.0         # GeoTIFF 多波段读取、图像金字塔(LOD)、视口裁剪
